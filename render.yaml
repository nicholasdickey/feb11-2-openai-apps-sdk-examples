services:
  - type: web
    name: pizzaz-mcp
    runtime: node
    plan: free

    buildCommand: pnpm install && pnpm run build:pizzaz
    startCommand: pnpm run start:pizzaz

    healthCheckPath: /health

    envVars:
      - key: NODE_ENV
        value: production
